import Styles from './styles.module.scss';

function Footer() {
	return (
		<footer className={Styles.footer}>
			<small className={Styles.byline}>Build by Edsel</small>
		</footer>
	);
}
export default Footer;
